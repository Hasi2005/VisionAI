name: visionai
description : A new Flutter project 
publish_to: 'none'
version : 0.0.1+1
environment:
  sdk: ^3.6.0
dependencies:
  flutter:
    sdk: flutter
 # camera: ^0.9.4+2
  #english_words: ^4.0.0 
  provider: ^6.1.2 
  camera: ^0.11.1
  path_provider: ^2.1.5
  path: ^1.9.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true  